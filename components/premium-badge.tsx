import premiumBadgeStyles from './premium-badge-styles.module.css'

const PremiumBadge = () => {
  return <span className={premiumBadgeStyles['premium-badge']}
  >💎</span>
}

export default PremiumBadge
